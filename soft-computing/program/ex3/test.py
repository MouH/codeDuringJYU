from nsga2 import *



def main():
    # node = Node(1)
    # print node.getRank()

    # x = Node([10,20])
    # y = Node([2,9])
    # z = Node([5,6])
    # pop = [x,y,z]

    # nsga2 = NSGA2(pop)

    # m = Node([30,1])
    # newpop = pop + [m]
    # # print len(newpop)
    # haha = nsga2.getNextPopulation(newpop)
    # for h in haha:
    # 	print h.getX()
    # nsga2.crowdingDistanceCompute(pop)
    # for node in pop:
    #     print node.getCrowdingDistance() 

    # nsga2 = NSGA2(pop)

    # for list in nsga2.nonDominatedSort(pop):
    #     # print 1
    #     for node in list:
    #         print node.getX(), node.getRank()
    # pass

if __name__ == '__main__':
    main()